
import React from "react";

export default function ({name}) {
    return (
        <div>
            {/*Create a React component called Greetings that displays a greeting message like "Hello, World!".*/}
    		<h1>Hello, World! {name}</h1>
        </div>
    );
}
